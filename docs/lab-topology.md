# Lab Topology

<!-- FILL IN once built. -->

## VM Inventory

| Role | Hostname | OS | IP (isolated network) |
|---|---|---|---|
| Domain Controller | | | |
| Windows Client | | | |
| Attacker | | | |

## Network Configuration

<!-- FILL IN: hypervisor (KVM/virt-manager), virtual network name and type
     (must be "Isolated network", not NAT or routed), subnet used, and
     confirmation that no route exists from this network to your host's
     normal LAN. -->

## Domain Structure

<!-- FILL IN: OUs created, notable accounts — including which service
     account was deliberately configured with a weak password for
     Kerberoasting, and your reasoning for how you set up the domain's
     complexity. -->
